#include <stdio.h>
/**
 * main - Prints the alphabets in reverse
 * Return: Always 0 (Success)
 */
int main(void)
{
	char t;

	for (t = 'z'; t >= 'a'; t--)
	putchar(t);
	{
	putchar('\n');
	}
	return (0);
}
